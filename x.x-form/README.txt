To run this

```
$ npm install
$ cd node_modules/@cycle/core
$ npm install
$ npm run prepublish
```

This is (probably) required due to some NPM shitty defaults about SSH repos
I don't even want to be aware of.